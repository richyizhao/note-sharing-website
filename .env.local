# Own user name and password
DB_URI="mongodb+srv://<user-name>:<user-password>@cluster0.dif0w.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0"

# Run the following command for random-hash $ openssl rand -base64 32
SESSION_SECRET="<random-hash>"
